# Sample Markdown

This file exists so `tests/validate.sh` can confirm **glow** renders Markdown.

- A list item
- Another with `inline code`

> A blockquote, to exercise styled rendering.

```python
print("fenced code block")
```
